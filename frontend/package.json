{
  "//_comment_1": "TODO: Frontend Dependencies and Scripts Configuration",
  "//_comment_2": "Use React + Vite with the following dependencies for Phase 1:",
  
  "//_dependencies_instructions": {
    "react": "^18.x (or 19.x)",
    "react-dom": "^18.x",
    "react-router-dom": "^6.x (For page routing and route protection)",
    "axios": "^1.x (For HTTP API communication with Spring Boot backend)",
    "lucide-react": "^0.x (For sleek UI vector icons)"
  },
  
  "//_devDependencies_instructions": {
    "vite": "^5.x",
    "@vitejs/plugin-react": "^4.x"
  },
  
  "//_scripts_instructions": {
    "dev": "vite (Start development server locally on http://localhost:5173)",
    "build": "vite build (Compile production bundle)",
    "preview": "vite preview (Preview local production build)"
  }
}
